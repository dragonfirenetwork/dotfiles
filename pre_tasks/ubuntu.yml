---
# pre_tasks/ubuntu.yml

- name: System Setup | Repositories | Add sources.list for ubuntu hosts
  copy:
    src: ubuntu_sources.list
    dest: /etc/apt/sources.list
    backup: yes
  notify: apt_update
  become: true
  when: ansible_distribution == "Ubuntu"
  tags:
    - pre_tasks