---
# roles/aliases/tasks/debian.yml

- name: Aliases | Copy .aliases file
  copy:
    src: aliases
    dest: "{{ ansible_env.HOME }}/.aliases"
    owner: "{{ ansible_env.USER }}"
    group: "{{ ansible_env.USER }}"
    mode: '0644'
  tags:
    - aliases

- name: Aliases | Source .aliases from .zshrc
  lineinfile:
    path: "{{ ansible_env.HOME }}/.zshrc"
    line: |
      # Load aliases
      source ~/.aliases
    insertafter: EOF
  tags:
    - aliases
